# Modelos GLB

Coloque aqui os arquivos `.glb` das peças (ex.: `motherboard.glb`, `socket.glb`,
`processor.glb`, `heatsink.glb`, `cooler.glb`). Veja a seção
"Trocando as peças placeholder por modelos GLB" no README da raiz.
